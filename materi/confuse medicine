let preCounter = 0;
console.log(++preCounter);

//Math.PI > untuk cari circle / 22/7 > 3.14
//Math.pow > untuk cari kuadrat

const i = 8;

const ai = Math.pow(i, 2); //>> 8x8
console.log(ai);

//Math.floor >> nilai numerik di bulatkan ke dalam bilangan bulat terbesar
//Math.ceil >> membulatkan nilai ke atas ke bilangan bulat terdekat
//Math.round >> membualtkan nilai ke bilangan bulat terdekat

//Math.abs >> mengembalikan nilai absolute

const number = 1;
const nilaiAbsolut = Math.abs(number);
console.log(nilaiAbsolut);

let age = 20;

if (age >= 17) {
  console.log(" you can make an ID card");
}

//if > di gunakan untuk mengevaluasi kondisi tunggal
//else if > di gunakan untuk mengevaluasi kondisi secara berurutan

const agee = 21;

if (age >= 18) {
  console.log(" can make an ID Card");
} else {
  console.log(" you cant make an ID card");
}

let score = "B";

if (score === "A") {
  console.log("excellent result");
} else if (score === "B");
{
  console.log("Great Result");
}

//swift case can permudah kita mengatur ulang program

//example export papaya, etc

// const exporttt = "Papayas";
// Switch (exporttt) {
//         case "Mango";
//     console.log("Mango are $0.029 a pound.");
//     break;
//     case "Papayas";
//     console.log("Mangos and Papayas are $2.8 a pound.");
//     break;
//     default:
//         console.log(`Sorry, we are out of ${export}.`);
// }

let s = []; // mendefinisikan sebuah array kosong
s.push(1); // menambah angka 1 ke dalam array s
s.push(2); // menambah angka 2 ke dalam array s
console.log(s); //output: [1,2]

/*

class > ES6, blueprint > template guna dimana saja class di inisiasi
class buat mpermudah kita dalam mengelola data, nilai, variable dan property


saat membuat object dilakukan dg manual dan berulang > is not good practice


kita hanya perlu membuat classnya saja dan mendefinisikan isi dari class tersebut
class untuk menjaga str

Di dalam class :
>method 
> data variable
>object


> mendefinisikan nama dan berikan property atau parameter
panggil class di ikuti dg parameter untuk membuat object secara otomatis


*/

/*
object > data berisi field / property / attribute atau method/  function/ behavior

class > blueprint, prototype > berisi deklarasi semua properties dan fungsi yang dimiliki oleh object
object selalu di buat dari klass.
sebuah clkass bisa membuat object untlimited

*/

//gitu mana yg msh otw project  u ta
